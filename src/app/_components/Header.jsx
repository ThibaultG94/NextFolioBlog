import React from 'react';
import Navigation from './Navigation';

const Header = () => {
	return (
		<header className="flex pt-8 pb-4 px-4 w-full">
			<Navigation />
		</header>
	);
};

export default Header;
